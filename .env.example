# Google Service Account JSON key file path (read by Rust backend — NEVER exposed to browser)
SERVICE_ACCOUNT_JSON=./service-account.json

# Your personal Gmail address — the spreadsheet and backup folder are automatically shared here
PERSONAL_GMAIL=your@gmail.com
